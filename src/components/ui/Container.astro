---
interface Props {
	as?: string;
	class?: string;
	narrow?: boolean;
}

const { as: Tag = "div", class: className = "", narrow = false } = Astro.props;
---

<Tag class:list={["mx-auto w-full px-5 sm:px-6 lg:px-8", narrow ? "max-w-4xl" : "max-w-7xl", className]}>
	<slot />
</Tag>
